# Copy to .env and fill values
SMTP_PASSWORD=YOUR_SENDGRID_API_KEY
EMAIL_FROM=you@yourdomain.com
PORT=8080
# Optional (not required by server.js but useful in other setups)
SMTP_HOST=smtp.sendgrid.net
SMTP_PORT=587
